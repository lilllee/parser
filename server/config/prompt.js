export const vllmPrompts = Object.freeze({
  imageAnalysis:
    "문서에서 추출한 이미지다. 아래 규칙대로 한국어로 간단히 설명한다.\n" +
    "1) 로고·심볼·엠블럼·직인/도장·서명·장식선·아이콘·머리글 장식처럼 정보 가치가 없는 이미지면, 다른 말 없이 정확히 \"NO_VISUAL\" 한 단어만 출력한다.\n" +
    "2) 차트·그래프로 데이터 값 라벨이 보이면, 그 값을 항목(행)×계열(열) markdown 표로 옮긴 뒤 축·범례·두드러진 값/추세를 1~2문장 덧붙인다.\n" +
    "3) 사진·삽화·다이어그램이면 핵심 내용만 1~2문장으로 설명한다.\n" +
    "형식: 표 외에는 평문 한 단락으로만 답한다. 제목(#)·구분선(---)·목록·굵게(**)·소제목 금지. '이 이미지는'·'분석하면' 같은 군더더기 없이 바로. 추측 금지, 보이는 것만.",

  tableAnalysis:
    "다음 markdown 표의 핵심 인사이트를 한국어로 2~3문장으로 요약해주세요. 숫자가 있으면 두드러진 값과 추세를 짚어주세요." + " 표 자체를 다시 옮기지는 마세요.",

  pageVisualAnalysis:
    '이 문서 페이지 이미지에서 아래 문맥과 관련된 그림/차트/그래프/통계 시각자료를 찾아 한국어로 2~4문장 분석해주세요. 축, 범례, 단위, 두드러진 값, 증감 추세, 비교 포인트가 보이면 구체적으로 적고, 보이지 않는 내용은 추측하지 마세요. ' +
    '만약 이 페이지에 분석할 그림/차트/그래프 시각자료가 없으면 다른 설명 없이 정확히 "NO_VISUAL" 이라고만 답하세요.',

  pageVisualContext(context) {
    return `관련 문맥:\n${context}`;
  },

  // 화살표 기호(⇨⇩⇦ 등)로 흐름을 나타낸 흐름도를 표로 떠온 것 → mermaid 변환(텍스트 입력).
  flowchartToMermaid:
    "다음은 화살표 기호(⇨ ⇩ ⇦ ⇧ → ↓ ← ↑ 등)로 단계 흐름을 나타낸 '흐름도'를 표로 옮긴 것이다. " +
    "셀의 단계 텍스트와 화살표 방향을 읽고 동등한 mermaid 흐름도로 변환한다.\n" +
    "- ```mermaid 코드블록 하나만 출력한다. 코드블록 밖 설명·문장은 쓰지 않는다.\n" +
    "- 가로 흐름이면 graph LR, 세로 흐름이면 graph TD. 화살표 방향대로 노드를 --> 로 잇는다.\n" +
    "- 노드 라벨은 셀의 실제 텍스트를 그대로 쓰고 한글 라벨은 큰따옴표로 감싼다(예: A[\"신청접수\"]). 부가 설명은 라벨 안에서 <br> 로 줄바꿈한다.\n" +
    "- 표에 없는 단계·연결을 지어내지 않는다. 방향이 불명확하면 읽는 순서(좌→우, 위→아래)를 따른다.\n" +
    "- 흐름도가 아니거나 변환이 불가능하면 다른 말 없이 정확히 NO_FLOWCHART 만 출력한다.",

  pdfOcrSystem: `PDF 페이지 이미지를 markdown 으로 정확히 옮긴다.
- 보이는 한국어/영어/숫자 그대로. 내용을 지어내지 않는다.
- 보이는 글자를 비슷한 다른 단어로 바꾸지 않는다(예: '동의서'를 '의뢰서'로 바꾸지 말 것). 불확실해도 보이는 형태 그대로 옮긴다.
- 소프트 줄바꿈은 한 문단으로 합치고 진짜 문단만 빈 줄로 구분. 줄끝 하이픈은 한 단어로 결합.
- 다단 컬럼은 읽는 순서(왼쪽 단 위→아래, 그다음 단)로.
- 제목은 #/##/### 헤딩, 목록은 -/1. 리스트. 표는 기본 markdown 파이프 표.
- 머리글이 2단 이상이어도 직사각형 표(셀이 모두 1×1)면 markdown 으로 그린다 — 상·하위 머리글은 한 칸으로 펴서(예: "2026년p 3월") 합치고 모든 행의 칸 수를 맞춘다. 단지 머리글이 2단이라는 이유로 HTML 을 쓰지 않는다.
- HTML <table>(colspan/rowspan)는 '데이터 본문에 진짜 병합 셀'이 있을 때만 쓴다 — 한 칸이 여러 데이터 행에 걸쳐 합쳐졌거나(rowspan), 한 값이 여러 데이터 열을 가로지를 때(colspan). markdown 칸에 'rowspan='·'colspan=' 텍스트는 절대 쓰지 않는다(필요하면 그 표만 HTML 로).
- 절대 금지: 설명 문장(□·○·▪ 로 시작하는 줄), 표·그림 캡션([표 N]·[그림 N]·"(단위: …)"), 또는 다른 표를 같은 표의 셀 안에 넣지 않는다 — 모두 표 밖 본문 줄로 둔다. 한 페이지에 표가 둘 이상이면 반드시 각각 별도 표로 그린다(하나로 합치지 말 것).
- 셀 안에 또 다른 표가 들어있으면(중첩 표) 바깥은 HTML <table>, 해당 <td> 안에 중첩 <table>로 넣는다(<br>+| 로 욱여넣지 말 것).
- 표 머리글(연도·항목명)은 값 칸과 정확히 대응시키고, 같은 표에서 연도·단위를 임의로 바꾸거나 안 보이는 값을 추정으로 채우지 않는다(빈 칸으로).
- 도장/서명은 [도장]/[서명], 손글씨도 가능한 한 옮긴다.
- 차트/그래프는 제목·단위(예: "(단위: %)")·출처(예: "자료: ...")를 옮기고, 값 라벨이 보이면 전부 표로 변환한다.
  · 범례가 여러 계열(예: 누적·그룹 막대의 색깔별)을 나타내면 각 계열을 별도 열로 만들고 머리글 첫 칸은 항목(연도 등), 다음 칸들은 범례 이름으로 단다. 예: | 연도 | OECD | Non-OECD |.
  · 누적막대는 막대 전체 합계가 아니라 색 구간 각각의 값을 따로 읽는다. 그룹막대는 나란한 막대 각각을 해당 계열 열에 넣는다.
  · 계열이 1개면 "항목|값" 2열. 한 그림이 여러 소차트면 소차트별로 표를 나누고 제목을 단다. 보이지 않는 값은 빈 칸(축 눈금으로 어림하지 않음).
- 페이지 상·하단에 반복되는 머리말·꼬리말(문서 제목·장 제목·페이지 번호)과 워터마크는 옮기지 않는다. 그 외 설명·요약·코멘트·해설 금지 — 보이는 내용만 그대로 전사한다. 그림/차트에 "> " 인용 분석을 붙이지 않는다(시각자료 해설은 이후 enrich 단계가 담당).`,

  pdfOcrUser(pageNumber) {
    return `페이지 ${pageNumber} 이미지입니다.`;
  },

  // 표가 깨진 출력 감지 후 재시도 시 user 프롬프트에 덧붙이는 강조 지시.
  pdfOcrTableRetry:
    "중요: 이 페이지의 표에 셀 병합(colspan/rowspan)이나 셀 안 중첩 표가 있으면 반드시 HTML <table>로 그린다(병합은 colspan/rowspan, 중첩 표는 <td> 안에 <table>). markdown 표 칸에 'rowspan='·'colspan=' 같은 텍스트를 쓰거나 <br>+| 로 표를 욱여넣지 말 것. HTML 표도 각 행의 유효 열 수가 헤더와 본문에서 반드시 같아야 한다. 왼쪽 '구분' 영역이 여러 시각 열(예: 대분류/직위/반)로 나뉜 표는 그 열들을 빠뜨리지 말고, '구 분' 헤더는 빈 <th>를 따로 만들지 말고 colspan 으로 정확히 합친다. markdown 표를 쓸 땐 모든 행의 칸 수가 동일해야 한다.",

  imageOcrSystem:
    `이미지의 모든 텍스트를 markdown 으로 정확히 옮긴다.
- 보이는 한국어/영어/숫자 그대로. 내용을 지어내지 않는다.
- 소프트 줄바꿈은 한 문단으로 합치고 진짜 문단만 빈 줄로 구분. 줄끝 하이픈은 한 단어로 결합. 다단은 읽는 순서대로.
- 제목은 #/##/### 헤딩, 목록은 -/1. 리스트, 표는 markdown 표.
- 도장/서명은 [도장]/[서명], 손글씨도 가능한 한 옮긴다.
- 차트/그래프는 제목·단위·출처를 옮기고, 값 라벨이 보이면 항목(행)×계열(열) 표로 변환. 축 눈금으로 어림잡지 않는다.
- 워터마크·장식 제외. 그 외 설명·요약·코멘트·해설 금지 — 보이는 내용만 그대로 전사한다. 그림/차트에 "> " 인용 분석을 붙이지 않는다(시각자료 해설은 이후 enrich 단계가 담당).`,

  imageOcrUser: "이미지입니다.",
});
