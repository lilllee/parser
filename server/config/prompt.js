export const vllmPrompts = Object.freeze({
  imageAnalysis:
    "다음 이미지를 한국어로 2~4문장 분석해주세요. 다이어그램이면 핵심 구성요소와 흐름을, 차트면 축·범례·두드러진 값/추세를, 표면 의미 있는 패턴을 짚어주세요. 추측은 피하고 보이는 것만 적으세요.",

  tableAnalysis:
    "다음 markdown 표의 핵심 인사이트를 한국어로 2~3문장으로 요약해주세요. 숫자가 있으면 두드러진 값과 추세를 짚어주세요. 표 자체를 다시 옮기지는 마세요.",

  pageVisualAnalysis:
    '이 문서 페이지 이미지에서 아래 문맥과 관련된 그림/차트/그래프/통계 시각자료를 찾아 한국어로 2~4문장 분석해주세요. 축, 범례, 단위, 두드러진 값, 증감 추세, 비교 포인트가 보이면 구체적으로 적고, 보이지 않는 내용은 추측하지 마세요. 만약 이 페이지에 분석할 그림/차트/그래프 시각자료가 없으면 다른 설명 없이 정확히 "NO_VISUAL" 이라고만 답하세요.',

  pageVisualContext(context) {
    return `관련 문맥:\n${context}`;
  },

  pdfOcrPage(pageNumber) {
    return `이 PDF 페이지 이미지의 내용을 깔끔한 markdown 으로 옮기세요. (페이지 ${pageNumber})
규칙:
- 한국어 / 영어 / 숫자를 페이지에 보이는 그대로 옮긴다. 내용을 지어내지 않는다.
- 한 문단 안에서 줄이 나뉘어 보여도 한 문단으로 합친다. PDF 의 시각적 줄바꿈(소프트 랩)은 제거하고, 진짜 문단이 바뀔 때만 빈 줄로 구분한다.
- 단어 끝에서 하이픈으로 줄이 나뉜 경우(예: "tech-\\ntnology")는 하이픈을 없애고 한 단어로 이어 붙인다.
- 2단/다단 컬럼은 사람이 읽는 순서(왼쪽 단을 위에서 아래까지 먼저, 그다음 오른쪽 단)대로 옮긴다.
- 제목 / 소제목은 수준에 맞춰 markdown 헤딩(#, ##, ###)으로 일관되게 표기한다.
- 글머리표 / 번호 목록은 markdown 리스트(-, 1.)로 표기한다.
- 표는 markdown 표로 변환한다.
- 도장 / 서명이 보이면 [도장] / [서명] 으로 표기하고, 손글씨도 가능한 한 정확히 옮긴다.
- 그림 / 차트 / 그래프 / 다이어그램이 있으면, 그 캡션·데이터를 옮긴 뒤 바로 아래에 "> " 인용으로 시작하는 2~3문장 한국어 설명을 덧붙인다. 축·범례·단위·두드러진 값·증감 추세·비교 포인트가 보이면 구체적으로 적고, 보이지 않는 내용은 추측하지 않는다.
- 페이지 번호, 머리말 / 꼬리말, 워터마크 같은 본문 외 요소는 제외한다.
- 위 그림/차트 설명을 제외하고는 일반 본문에 설명·요약·코멘트를 덧붙이지 않는다 — 페이지 내용 그대로만 출력한다.`;
  },

  imageOcr:
    `이 이미지에 보이는 모든 텍스트를 깔끔한 markdown 으로 옮기세요.
규칙:
- 한국어 / 영어 / 숫자를 이미지에 보이는 그대로 옮긴다. 내용을 지어내지 않는다.
- 한 문단 안에서 줄이 나뉘어 보여도 한 문단으로 합친다(소프트 줄바꿈 제거). 진짜 문단이 바뀔 때만 빈 줄로 구분한다.
- 단어 끝에서 하이픈으로 줄이 나뉜 경우는 하이픈을 없애고 한 단어로 이어 붙인다.
- 다단 컬럼은 사람이 읽는 순서대로 옮긴다.
- 제목/소제목은 markdown 헤딩(#, ##, ###), 글머리표/번호 목록은 markdown 리스트(-, 1.), 표는 markdown 표로 표기한다.
- 도장/서명이 보이면 [도장]/[서명] 으로 표기하고, 손글씨도 가능한 한 정확히 옮긴다.
- 차트/그래프/다이어그램이 있으면 캡션·데이터를 옮긴 뒤 바로 아래에 "> " 인용으로 2~3문장 한국어 설명을 덧붙인다. 보이는 것만 적고 추측하지 않는다.
- 워터마크 / 장식 등 본문 외 요소는 제외한다. 위 그림 설명 외에는 설명·요약·코멘트를 덧붙이지 않는다 — 이미지 내용 그대로만 출력한다.`,
});
